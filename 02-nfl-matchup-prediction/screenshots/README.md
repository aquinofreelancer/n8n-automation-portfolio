# Screenshots

Place redacted n8n canvas screenshots here. Suggested captures:

1. **Full workflow canvas** — showing parallel ingestion branches and merge
2. **Cron trigger** — weekly schedule configuration
3. **HTTP request nodes** (one per source) — with URL redacted
4. **LLM sentiment node** — showing the structured output schema
5. **Merge node** — showing three-input configuration
6. **Per-matchup splitter** — showing the loop/iteration pattern
7. **Gmail send node** — with recipient redacted

## Redaction checklist

Before committing any screenshot:

- [ ] API keys / tokens replaced with `***REDACTED***` or pixelated
- [ ] Endpoint URLs replaced with `https://api.example.com/...`
- [ ] Recipient email replaced with `example@domain.com`
- [ ] Credential dropdown names visible but values never shown

Save screenshots as `.png` with descriptive filenames like `01-full-canvas.png`, `02-parallel-fanout.png`, etc.
